# AgentEval：多框架 Agent 工具调用评测体系 — 项目方案

> 版本：v1.0（RL 闭环不在本期范围）
> 状态：待评审 / 待启动
> 预计工期：4 周（业余时间约 20~30 小时/周）

---

## 1. 项目定位与背景

### 1.1 一句话定位

一个**轻量、可复现、多框架横向对比**的 LLM Agent 评测体系，任务域聚焦**多轮工具调用**，产出量化结果与失败模式分析。

### 1.2 解决的问题（为什么值得做）

现有 Agent 评测方案各有明显短板：

| 现有方案 | 短板 |
|---|---|
| BFCL 等工具调用榜单 | 单轮、单一工具选择，不覆盖多轮状态依赖与规划 |
| SWE-bench | 计算成本高、赛道拥挤，个人项目难以做出新意 |
| WebArena | 需自建网站容器环境，基础设施极重 |
| τ-bench | 优秀但封闭，只评自己框架的模型，不支持"横向对比框架" |
| 各类评测框架（如 OpenHands 自带评测） | 绑定单一框架，无法跨框架控制变量对比 |

**本项目的差异化**：在"任务集 — 统一 harness — 多框架适配 — 多指标 — 失败模式分析"五个环节上都保持轻量与透明，让"同一个任务、同一个模型，不同 Agent 框架"的控制变量对比成为可能。

### 1.3 与个人经历的关系（简历叙事）

- 三星实习（数据工程 + 时间序列大模型）→ 体现工程与数据处理能力
- 云知声实习（后训练 + 改造开源 Agent 并部署）→ 体现训练与 Agent 工程能力
- **本项目（评测体系）→ 补上"度量 Agent"这一块唯一缺失的硬能力**，把"做 Agent"升级为"做 Agent + 度量 Agent"的完整闭环

### 1.4 项目名称与仓库

- 项目名：**AgentEval**（暂定）
- 仓库：新建公开 GitHub 仓库（README 双语，中文为主）
- License：MIT

---

## 2. 项目目标与验收标准

### 2.1 量化目标

| 目标 | 指标值 |
|---|---|
| 任务集规模 | ≥ 40 个多轮工具调用任务，全部带**可执行 verifier** |
| 任务域覆盖 | 3 个业务子域，3 档难度（L1/L2/L3） |
| 接入框架数 | 3 个：自研 ReAct、smolagents、OpenHands |
| 指标完备度 | 成功率、工具调用 F1、轨迹分（LLM-as-judge）、成本、延迟 |
| 实验规模 | ≥ 2 个 backbone 模型 × 3 框架 = 6 组对照实验 |
| 交付物 | 仓库 + 实验报告 + Streamlit dashboard + 失败模式分析 |

### 2.2 验收标准（做完的标准，不是"最好"的标准）

1. 任何第三方按照 README 可以在 30 分钟内复现**一组完整实验**（不含 API key 申请时间）
2. 每个任务都有 verifier 单元测试，`pytest` 全绿
3. 主结果表至少包含：每框架 × 每模型的 SR、F1、轨迹分、成本、延迟 5 个指标
4. 失败模式分析至少定位 3 类可命名、可举例的失败模式
5. dashboard 可交互查看任意一次运行（run）的完整轨迹

---

## 3. 范围（In / Out）

### 3.1 本期做（In Scope）

1. 多轮工具调用任务集设计与 verifier 体系
2. 有状态的 mock 工具服务（FastAPI）
3. 统一评测 harness + 3 个框架适配器
4. 5 类评测指标 + LLM-as-judge 评分器
5. 实验管理与结果产出（YAML 配置、JSONL 轨迹、CSV 汇总）
6. 失败模式聚类分析
7. Streamlit dashboard
8. README / 复现指南 / 结果报告

### 3.2 本期不做（Out of Scope，明确记录）

| 不做的事 | 原因 / 后续计划 |
|---|---|
| RL 闭环（评测反馈微调） | v2 计划，需要 GPU 与训练时间 |
| 通用评测框架（插件注册、多租户） | 本项目的价值是"具体对比结果"而非框架本身 |
| Web / 代码 / GUI 任务域 | 基础设施过重，超出 4 周 |
| 真实外部 API 集成（真实支付等） | mock 服务已足够验证方法 |
| 分布式 / 并发大规模跑批 | 单机串行 + 少量并发即可 |
| 在线 leaderboard 托管 | 静态结果页 + dashboard 本地运行即可 |

---

## 4. 任务集设计（项目灵魂）

### 4.1 三个业务子域

每个子域一组工具 + 一组任务，共享同一 mock 服务：

| 子域 | 工具数 | 任务场景举例 | 状态特征 |
|---|---|---|---|
| ① 差旅助手（travel） | 7 | 查航班 → 订票 → 改签/退票 | 有状态（预订创建后状态机流转） |
| ② 电商客服（shop） | 6 | 查订单 → 修改地址 → 申请退款 | 有状态 + 权限（仅限下单用户本人） |
| ③ 数据查询（analytics） | 5 | 多步 SQL 式查询 → 汇总出报告 | 只读为主，多跳依赖 |

设计要点：
- **状态性**：`book_flight` 之后系统状态改变，后续任务依赖前序结果 → 天然形成多轮任务
- **约束性**：退款需要先验证身份、改签有票价差规则 → 产生"错误工具序列"
- **确定性**：所有工具返回固定数据（无随机），保证 verifier 与复现

### 4.2 任务 Schema

```json
{
  "task_id": "travel_014",
  "domain": "travel",
  "user_goal": "帮我预订 10 月 12 日北京到上海上午的航班，经济舱；如果价格超过 1200 元，改订同一天最便宜的航班并告诉我价格。",
  "difficulty": "L2",
  "design_note": "考察条件分支：上午航班超预算时切换最便宜方案的决策能力",
  "min_expected_turns": 2,
  "max_steps": 12,
  "initial_state": { "user_id": "u_42", "vip": false },
  "required_calls": [
    { "tool": "search_flights", "args_contain": { "date": "2026-10-12", "from": "PEK", "to": "SHA" } }
  ],
  "forbidden_calls": [],
  "final_state_checks": [
    { "path": "reservations.u_42.0.class", "equals": "economy" },
    { "path": "reservations.u_42.0.status", "equals": "booked" }
  ],
  "answer_checks": [
    { "contains": "1200" }
  ],
  "ground_truth_plan": [
    { "tool": "search_flights", "args": { "date": "2026-10-12", "from": "PEK", "to": "SHA" } },
    { "tool": "book_flight", "args": { "flight_id": "MU5101", "class": "economy" } }
  ],
  "judge_rubric_hint": "重点考察：是否比较了价格并做出条件分支决策"
}
```

### 4.3 难度分级（各档定义与配比）

| 难度 | 定义 | 数量 |
|---|---|---|
| L1 | 单轮、单一工具即可完成（如"查一下明天的航班"） | 15 |
| L2 | 多轮状态依赖 + 条件分支（先查再订、超预算换方案） | 15 |
| L3 | 多工具组合 + 约束推理 + 干扰信息（如"帮我退掉最贵的那张票，但我忘了订单号"） | 10 |
| 合计 | | **40** |

### 4.4 Verifier 设计（任务是否成功的唯一权威判定）

四种检查原语，全部**确定性**执行：

| 检查类型 | 说明 | 示例 |
|---|---|---|
| `required_calls` | 必须发生的关键工具调用（含参数包含匹配） | 必须调用过 `search_flights` 且 date 正确 |
| `forbidden_calls` | 禁止发生的调用（权限/规则违反） | 未验证身份前禁止 `refund_order` |
| `final_state_checks` | 终态检查：mock 服务最终状态必须满足 | 预订存在、舱位正确、状态 booked |
| `answer_checks` | 最终回复文本的关键信息检查 | 回复中包含价格数字 |

判定：`passed = required 全满足 ∧ forbidden 全未发生 ∧ final_state 全满足 ∧ answer 全满足`

工程要求：
- 每个 verifier 配套 **pytest 单元测试**（正向用例 + 反向用例）
- verifier 与 mock 服务的状态读取走同一接口，避免不一致

### 4.5 样本来源与构造流程（评测样本从哪来）

一个"样本"= 任务定义 + 初始状态 + 可执行 verifier + 参考答案计划，四件套缺一不可（schema 见 §4.2）。四个来源按优先级组合（陷阱设计方法论、手把手示例与 LLM 扩写模板见同目录《任务设计指南.md》）：

| 来源 | 说明 | 占比目标 | 注意 |
|---|---|---|---|
| ① 人工手写种子 | 作者亲自设计每域 5 个典型任务，覆盖成功路径与陷阱 | ~30% | 项目独特性的核心，面试可逐题讲解设计意图 |
| ② LLM 辅助扩写 | 用模板 prompt 在种子基础上生成变体（换日期/用户/金额/约束条件） | ~50% | 生成后必须过校验清单，禁止直接入库 |
| ③ 借鉴公开 benchmark 结构 | τ-bench（verifier 判定模式）、BFCL（参数校验思路）、ToolBench（真实 API 任务）、MCP-Bench（MCP 工具任务）、AppWorld（函数调用世界）——**只借鉴任务结构，内容全部自己重写** | ~20% | 内容照搬有 license 与辨识度问题；借鉴结构 + 重写内容完全合法且能学到东西 |
| ④ 实习场景脱敏还原 | 把云知声 Agent 平台遇到的真实用户请求，脱敏改写为任务 | 视情况 | 加分项：真实性最高；只保留抽象诉求，注意公司数据保密 |

构造流程（每个任务必须走完全部四步）：

1. 产出任务草稿（来源①③④之一）或生成变体（来源②）
2. 实现 verifier 与 ground_truth_plan
3. **校验清单**逐项通过：
   - [ ] goal 表述无歧义，一个正常人能理解要做什么
   - [ ] verifier 在"空轨迹"上返回 fail（防假通过）
   - [ ] verifier 在 ground_truth_plan 上返回 pass（防假失败）
   - [ ] 无工具描述泄露答案（如任务描述里不能直接给订单号）
4. 入库 `tasks/v1` 并编写 verifier 单元测试

**为什么 40 个就够**：评测样本 ≠ 训练样本——每个样本带确定性验证器、人工校验成本高。40 个多轮任务 × 6 组配置 = 240 次运行，足以产出有统计意义的对比表与失败模式分布。样本数量后期可低成本叠加，但单个样本的 verifier 质量无法事后补救，所以**先保质量再谈数量**。

**样本的二次价值（v2 RL 铺垫）**：评测运行产生的全部轨迹（JSONL）本身就是高质量样本资产——失败轨迹自带失败原因标注，未来 RL 闭环的 reward 信号与正负样本都从这批轨迹中蒸馏，无需另找数据。

---

## 5. Mock 工具服务设计

### 5.1 技术方案

- FastAPI 单进程应用，内存态 + JSON 持久化（实验可复现时从 JSON 恢复初始状态）
- 每个任务实例独立状态空间：`POST /instances/{instance_id}/reset` 初始化
- 工具注册表使用 JSON Schema 风格定义（与 OpenAI function calling 兼容），三域共 17 个工具

### 5.2 接口设计

```
POST /instances/{instance_id}/reset          # 初始化/重置状态（body: initial_state）
POST /instances/{instance_id}/tools/{name}   # 调用工具（body: arguments）
GET  /instances/{instance_id}/state          # 读取当前状态（verifier 使用）
GET  /instances/{instance_id}/log            # 调用日志（含参数、返回、时间戳、调用方）
```

### 5.3 工具定义示例

```json
{
  "name": "refund_order",
  "description": "为订单申请退款。仅订单所属用户本人且已完成身份验证时可用。",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": { "type": "string" },
      "reason": { "type": "string" }
    },
    "required": ["order_id"]
  },
  "permission": "requires_auth",
  "read_only": false
}
```

### 5.4 关键设计点

- **权限与副作用标记**：每个工具声明 `permission` 与 `read_only`，供评测编排器与失败模式分析使用
- **错误注入（可选增强）**：L3 任务可配置"首次调用返回参数错误提示"，考察 Agent 的错误恢复
- **确定性**：同一 `(instance_id, tool, args)` 永远返回同一结果

---

## 6. 评测 Harness（评测编排器）设计

> **术语澄清**："harness" 在本项目中有两种含义，勿混淆：
> - **Agent harness（被评对象）**：运行 Agent 的框架/运行时，如 Claude Code、DeepSeek Harness、OpenClaw、OpenHands、smolagents——即 §6.5 中通过"适配器"接入的三个被评框架；"选 Agent 框架"属于选被试，不是本节的评测装置。
> - **Evaluation harness（评测装置）**：把被评 Agent 架到任务集上运行并计算指标的脚手架（如 lm-evaluation-harness、SWE-bench harness）。**本节（§6）讲的是这一层**。
>
> 两者关系：评测装置通过子进程驱动被评对象完成任务。类比：评测装置是"测试台"，被评 Agent 框架是"被测发动机"，CLI 契约是统一法兰接口。

### 6.1 核心抽象

```python
@dataclass
class Step:
    type: str            # "message" | "tool_call" | "observation"
    role: str            # "user" | "assistant" | "tool"
    content: str
    tool_name: str | None
    tool_args: dict | None
    ts: float            # 时间戳
    tokens_in: int
    tokens_out: int
    cost_usd: float

@dataclass
class Trace:
    run_id: str
    task_id: str
    framework: str       # "react" | "smolagents" | "openhands"
    model: str
    steps: list[Step]
    final_answer: str
    status: str          # "completed" | "timeout" | "error" | "budget_exceeded"
    wall_time_s: float
    total_cost_usd: float

# harness 是"薄编排器"：通过子进程驱动各适配器，不在库内直接调用（架构选型见 §6.2）
def run_one(task: Task, adapter_cmd: list[str], model: str, budget: RunBudget) -> Trace:
    """spawn 子进程完成单任务评测；超时/崩溃/预算超限统一按 fail 处理并保留轨迹"""
```

### 6.2 架构选型：薄编排器 + 子进程适配器（推荐）

三种候选架构对比：

| 架构 | 优点 | 致命问题 | 结论 |
|---|---|---|---|
| A. 库内直接调用（import adapter 类） | 实现最简、易调试 | smolagents/openhands 依赖重、易冲突；一次崩溃污染整个评测进程；异步事件循环难共存 | ❌ |
| B. HTTP 服务化（适配器常驻服务） | 隔离性好 | 服务治理/端口/生命周期复杂度高，对本项目过度设计 | ❌ |
| C. **子进程 + 统一 CLI 契约** | 崩溃隔离、超时可强杀、依赖天然隔离、适配器可独立调试 | 需定义进程间数据契约（成本低） | ✅ 采用 |

选 C 的理由：SWE-bench / AgentBench 等主流评测 harness 均为此模式；本项目三框架依赖差异大（OpenHands 尤其重），子进程隔离是唯一不陷入工程泥潭的方式；"一次运行 = 一个子进程"使超时、重试、断点续跑全部变成文件级操作。

### 6.3 Adapter CLI 契约（三个框架都要实现的统一接口）

每个适配器是一个可独立执行的 CLI，harness 以 `subprocess` 启动：

```bash
python -m harness.adapters.<name> --spec run_spec.json --out trace.jsonl
```

`run_spec.json`（harness 生成，适配器只读）：

```json
{
  "run_id": "r_001",
  "task": { "task_id": "travel_014", "user_goal": "...", "difficulty": "L2" },
  "tool_specs": [{ "name": "search_flights", "description": "...", "parameters": {} }],
  "tool_server": { "base_url": "http://127.0.0.1:8200", "instance_id": "r_001" },
  "model": "deepseek-v4-flash",
  "model_params": { "temperature": 0.2 },
  "budget": { "max_steps": 12, "timeout_s": 180, "max_cost_usd": 0.05 }
}
```

适配器职责：把统一 `tool_specs` 翻译成各自框架的工具格式、驱动运行、**直接写出统一格式的 `trace.jsonl`**、原始日志另存 `trace.raw.log`。退出码约定：`0` 正常结束；`2` 预算/超时；`1` 运行错误（harness 均按 fail 计并保留已产出轨迹）。

归一化责任下放到适配器（共享 `harness.protocol` 数据类），harness 不再理解任何框架细节——这是"薄编排器"的关键。

### 6.4 运行预算与失败处理

| 预算项 | 默认值 | 超出后 |
|---|---|---|
| `max_steps` | 12（任务 schema 可覆盖） | 状态置 `budget_exceeded`，按 fail 计 |
| `timeout_s` | 180 | 状态置 `timeout`，按 fail 计 |
| `max_cost_usd` | 0.05 | 中止，按 fail 计 |
| 异常/崩溃 | — | 状态置 `error`，保留已捕获轨迹 |

### 6.5 三个适配器方案

三个适配器均实现 §6.3 的 CLI 契约，可脱离评测编排器单独运行与调试。

**① 自研 ReAct（react.py，~200 行，零框架依赖）**
- 循环：模型输出 → 解析工具调用（JSON 格式约束 prompt）→ 调 mock 服务 → 回填 observation → 直至输出最终答案或超预算
- 直接走 LiteLLM 的 `completion(..., tools=[...])` 或纯 JSON 解析（二选一，实现时先纯 JSON，兼容性最好）
- 价值：control group + 展示"你会写 Agent"

**② smolagents（smolagents.py）**
- 使用官方 `CodeAgent`（代码生成式工具调用），模型经 `LiteLLMModel` 注入
- 工具用 `@tool` 装饰器包裹 mock 服务 HTTP 调用
- 风险低：库成熟、文档全

**③ OpenHands（openhands.py，本期最大技术风险点）**
- 方案 A（首选）：OpenHands Python SDK headless 模式，注册自定义工具后运行任务
- 方案 B（降级）：固定版本 Docker 镜像 + CLI 模式，脚本化控制
- 方案 C（兜底）：若两周内无法稳定跑通，替换为 **OpenManus 同源的轻量框架或 LangGraph 预构建 Agent**——但替换必须保证"重框架"对比位不变，且需重新评估与原方案的可比性
- 时间盒：W3 前必须跑通 1 个任务，否则触发方案 B/C

### 6.6 双源轨迹采集（适配器埋点难点的解耦方案）

各 Agent 框架的事件流/回调机制差异很大，逐一埋点成本高且脆弱（OpenHands 尤其如此）。采用**双源采集、归并对齐**：

| 来源 | 记录内容 | 权威性 |
|---|---|---|
| mock 工具服务 `/log` | 每次工具调用的完整请求/响应/时间戳 | **工具调用以服务端日志为准**（不受框架埋点质量影响） |
| 适配器侧 | 模型消息、步骤时间戳、token 用量、最终答案、异常 | 消息与成本以适配器为准 |

- `merge.py` 按 `run_id`（= `instance_id`）与时间戳把两源归并成统一 `Trace`
- 收益：即使某框架事件流不完整，工具侧数据依然完整可用；服务端日志天然佐证轨迹真实性
- 校验：归并后检查服务端工具调用数与轨迹 `tool_call` 步数一致，不一致打 warning 并保留双源原始数据

### 6.7 复现性保障

- 实验配置集中在 `configs/*.yaml`：模型、参数（temperature=0.2）、seed、预算、适配器版本
- OpenHands 固定镜像 tag；所有 Python 依赖用 `uv.lock` 锁版本
- 每次运行产生唯一 `run_id`，轨迹与结果按 `run_id` 归档，绝不覆盖

---

## 7. 评测指标设计

### 7.1 指标总览

| 指标 | 类型 | 定义 | 主要用途 |
|---|---|---|---|
| Task Success Rate (SR) | 硬指标 | verifier 判定 passed 的任务占比 | 核心结论 |
| Tool-Call F1 | 硬指标 | 工具调用的查全/查准（见 7.2） | 定位"错在哪" |
| Trajectory Score | 软指标 | LLM-as-judge 1~5 分 | 效率与合理性 |
| Cost | 硬指标 | 每任务平均 $（LiteLLM 用量统计） | 工程化视角 |
| Latency | 硬指标 | 端到端耗时 + 工具往返耗时 | 工程化视角 |
| Step Efficiency | 派生 | 成功任务的平均步数 | 辅助分析 |

### 7.2 Tool-Call F1 定义（明确口径）

与 `ground_truth_plan` 对比，**以"关键调用"为单位**：

- **Recall**：`ground_truth_plan` 中每个关键调用，在真实轨迹中是否出现了"同工具 + 参数包含匹配"的调用 → 出现比例
- **Precision**：真实轨迹中的每次工具调用，是否属于 `ground_truth_plan ∪ 任务 schema 声明的允许工具集`，且参数格式合法 → 合法比例
- **F1 = 2PR/(P+R)**

说明：该口径对"多解任务"（达到目标但路径不同）公平——Recall 只看关键依赖，Precision 允许等价工具集。

### 7.3 成本与延迟口径

- Cost：LiteLLM `completion_cost()` 汇总（含重试），mock 服务调用不计模型成本但记录耗时
- Latency：`wall_time_s` 端到端；工具往返耗时单独统计（observation 时间戳差）

---

## 8. LLM-as-Judge 设计

### 8.1 评分对象与输入

- 评分对象：**完整轨迹 + 最终答案**（一次运行评一次，不做逐步评分，控制成本）
- 输入构造：任务描述 + 压缩轨迹（长 observation 截断为前 500 字符 + 摘要标注）+ rubric

### 8.2 Rubric（1~5 分）

| 分 | 定义 |
|---|---|
| 5 | 目标完全达成，动作无冗余，无错误尝试 |
| 4 | 目标达成，有轻微冗余（1 次无效调用） |
| 3 | 目标达成，但路径明显低效（≥2 次无效调用或走弯路） |
| 2 | 目标未达成，但方向基本正确（关键调用发生） |
| 1 | 目标未达成且路径错误（关键调用未发生/违反约束） |

### 8.3 防偏与质量控制

1. **匿名化**：轨迹输入给 judge 前移除框架名、模型名
2. **双 judge**：两个不同模型（如 GPT-4o + Qwen-Max 或 DeepSeek）独立评分
3. **一致性报告**：报告两 judge 的 agreement%（完全一致/±1 分一致），主表使用均值
4. **锚定**：随机抽取 10 条轨迹人工打分，作为 judge 质量参照（校验 judge 与人工分的相关性）
5. **失败兜底**：judge 输出非 JSON/无分数时重试 1 次，仍失败则回退为 verifier 硬指标（passed=5, failed=1）

---

## 9. 实验设计

### 9.1 实验矩阵

| 组 | 框架 | backbone 模型 | 说明 |
|---|---|---|---|
| R1 | react | deepseek-v4-flash | 主 backbone |
| R2 | react | Qwen2.5-72B-Instruct | 消融：换模型 |
| S1 | smolagents | 同主 backbone | 框架对比 |
| S2 | smolagents | Qwen2.5-72B-Instruct | 消融 |
| O1 | openhands | 同主 backbone | 框架对比 |
| O2 | openhands | Qwen2.5-72B-Instruct | 消融 |

规模：6 组 × 40 任务 = **240 次运行**。任务按 seed 固定顺序，每组跑一遍。

### 9.2 控制变量清单

- 三框架共用同一任务集、同一 mock 服务、同一工具描述文本（翻译为各自格式时保持语义一致）
- 模型参数统一：`temperature=0.2`（OpenHands 不支持时记录差异）
- 运行预算统一（§6.4 默认值）

### 9.3 结果产出（报告章节结构）

1. **主表**：6 组 × {SR, F1, Trajectory Score, Cost/任务, Latency} 
2. **分难度表**：L1/L2/L3 分别的 SR（看框架差异集中在哪档难度）
3. **失败模式分布**：失败任务按错误类型聚类（见 §10）
4. **成本效率散点**：Cost-SR 二维图（性价比视角）
5. **案例研究**：每框架挑 1~2 个典型轨迹做逐步分析（成功 1 例 + 失败 1 例）
6. **judge 质量报告**：双 judge 一致性、人工锚定对比

### 9.4 成本估算（预算上限）

| 项 | 估算 |
|---|---|
| 240 次运行 × 平均 ~8K tokens | deepseek-v4-flash（低价高吞吐），约 ¥20~60 |
| judge：240 次 × 2 模型 × ~2K tokens | 约 ¥20~50 |
| OpenHands 运行环境（沙箱/镜像） | 本机 Docker，¥0 |
| **总预算上限** | **¥200（含失败重跑余量）** |

---

## 10. 失败模式分析

### 10.1 分类体系（先验假设，实验后修正）

| 类别 | 定义 | 检测方式 |
|---|---|---|
| 工具选择错误 | 调用了错误的工具 | F1 Precision 低 |
| 参数错误 | 工具对了、参数错（格式/取值） | verifier required_calls 未命中 |
| 规划失败 | 缺少必要步骤 / 步骤顺序错 | Recall 低 + 终态检查失败 |
| 权限/规则违反 | 触发 forbidden_calls | forbidden 命中 |
| 预算耗尽 | 步数/超时/成本超限 | status ≠ completed |
| 解析失败 | 模型输出无法解析为工具调用 | 轨迹中出现解析错误标记 |

### 10.2 产出

- 失败任务 × 类别分布表（分框架）
- 每类别 1 个代表性轨迹节选（进报告案例研究）
- "框架 × 失败模式"交叉矩阵：看每个框架的**特征性弱点**（如 OpenHands 步数多但恢复强、ReAct 参数错误多等）——这是报告最有价值的一页

---

## 11. Trace 与数据格式

### 11.1 目录约定

```
runs/
  2026-xx-xx_react_deepseek/          # run 目录
    config.yaml                        # 本次运行配置快照
    traces/{task_id}.jsonl             # 归一化轨迹
    raw/{task_id}.{ext}                # 适配器原始日志
    results.csv                        # 本次运行结果
    summary.json                       # 汇总指标
```

### 11.2 JSONL 轨迹示例（一行一步）

```json
{"run_id":"r_001","task_id":"travel_014","framework":"react","model":"deepseek-v4-flash",
 "step":3,"type":"tool_call","tool_name":"search_flights",
 "tool_args":{"date":"2026-10-12","from":"PEK","to":"SHA"},
 "ts":1720000001.23,"tokens_in":1200,"tokens_out":45,"cost_usd":0.0006}
```

---

## 12. 项目结构（完整目录树）

```
agent-eval/
├── README.md                      # 项目说明 + 复现指南（双语）
├── LICENSE                        # MIT
├── pyproject.toml                 # uv 管理，锁 uv.lock
├── configs/
│   ├── experiment_matrix.yaml     # 6 组实验矩阵
│   └── models.yaml                # 模型与价格配置
├── tasks/
│   ├── v1/
│   │   ├── travel_tasks.json
│   │   ├── shop_tasks.json
│   │   └── analytics_tasks.json
│   ├── verifiers.py               # 四类检查原语的实现
│   └── validate.py                # 任务集校验工具（空轨迹 fail / GT pass）
├── tool_server/
│   ├── app.py                     # FastAPI 入口
│   ├── tools/                     # travel.py / shop.py / analytics.py
│   ├── registry.py                # 工具注册表（JSON Schema）
│   └── state.py                   # 状态管理（内存 + JSON 持久化）
├── cli.py                         # 统一入口：agent-eval run / report / dashboard
├── harness/
│   ├── orchestrator.py            # 薄编排器：调度、预算、超时强杀、断点续跑
│   ├── protocol.py                # Trace/Step/RunBudget + run_spec 契约 schema
│   ├── adapters/
│   │   ├── react.py               # 各适配器自带 CLI 入口（python -m harness.adapters.xxx）
│   │   ├── smolagents.py
│   │   └── openhands.py
│   └── merge.py                   # 双源轨迹归并（服务端工具日志 + 适配器消息日志）
├── metrics/
│   ├── success.py                 # verifier 判定
│   ├── tool_f1.py
│   ├── cost_latency.py
│   └── aggregate.py               # CSV/summary 产出
├── judge/
│   ├── rubric.py                  # rubric 常量
│   ├── prompts.py
│   ├── judge.py                   # 双 judge + 一致性
│   └── compress.py                # 轨迹压缩（长 observation 截断）
├── analysis/
│   ├── failure_modes.py           # 失败分类
│   └── report.py                  # markdown 报告生成
├── dashboard/
│   ├── app.py                     # Streamlit 入口
│   └── pages/                     # 总览 / 轨迹回放 / 失败分析
├── runs/                          # 实验产物（gitignore，GitHub 只留样例）
├── tests/
│   ├── test_verifiers.py
│   ├── test_tool_server.py
│   ├── test_react_adapter.py
│   └── test_metrics.py
└── docs/
    └── ARCHITECTURE.md            # 架构说明（mermaid 图）
```

---

## 13. 技术栈与开发环境

| 类别 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11 | smolagents/OpenHands 兼容性最稳 |
| 包管理 | uv | 快、锁版本（uv.lock） |
| 模型调用 | LiteLLM | 统一 API + 自带用量/成本统计；deepseek-v4-flash 若未收录则走 openai 兼容端点 |
| 服务 | FastAPI + uvicorn | mock 工具服务 |
| 数据 | pydantic v2 + JSONL + pandas | schema 校验 + 结果分析 |
| Agent 库 | smolagents / openhands | 两个被评对象 |
| CLI 编排 | typer | `agent-eval run/report/dashboard` 统一入口 |
| Dashboard | Streamlit | 两天出可用 UI |
| 可视化 | plotly | 交互式图表 |
| 测试/质量 | pytest + ruff | CI 可配 GitHub Actions（可选） |

---

## 14. 里程碑计划（4 周）

### W1：地基 + 最小闭环（本周末验收：一次完整评测跑通）

- [ ] 仓库初始化（uv、ruff、pytest、目录骨架）
- [ ] mock 工具服务：travel 域 6 个工具 + 状态管理 + reset
- [ ] 任务 schema 定稿 + travel 域 8 个任务（L1×3, L2×3, L3×2）+ verifier + 单元测试
- [ ] 自研 ReAct adapter（实现 §6.3 CLI 契约）+ LiteLLM 接入 + 编排器 v0（子进程调度/超时强杀）
- [ ] Trace 落盘与 results.csv 汇总（先只算 SR + cost + latency）
- **验收**：ReAct 单框架跑完 8 任务，产出第一份 results.csv

### W2：任务集扩充 + 第二框架 + 指标完备

- [ ] shop / analytics 两域工具与任务，总量 ≥ 25（含 W1 的 8 个）
- [ ] smolagents adapter 接入
- [ ] Tool-Call F1 实现（口径按 §7.2）
- [ ] 第一轮正式实验：R1/S1 两组（主 backbone）
- [ ] 失败模式分类 v0 + 第一次分布统计
- **验收**：R1/S1 两组 × 25 任务跑完，SR/F1/成本/延迟四指标齐

### W3：OpenHands + judge + 全量实验

- [ ] OpenHands adapter（含 §6.5 方案 A→B/C 的时间盒决策）
- [ ] judge 模块（双 judge + 一致性 + 锚定 10 条人工分）
- [ ] 任务集扩到 40，全部通过校验清单
- [ ] 全量 6 组 × 40 任务实验 + 结果复核
- **验收**：主表 + 分难度表 + 失败模式分布初版

### W4：分析、可视化、交付

- [ ] 失败模式聚类定稿 + 案例研究（每框架 2 例轨迹分析）
- [ ] Streamlit dashboard（总览页 / 轨迹回放页 / 失败分析页）
- [ ] 报告生成（markdown 主报告 + README 复现指南）
- [ ] judge 质量报告、成本汇总
- [ ] 代码清理、补测试、GitHub 发布（含样例 runs/ 数据）
- **验收**：陌生人 30 分钟复现一组实验；README + 报告 + dashboard 三件套齐

---

## 15. 风险与应对

| # | 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|---|
| 1 | OpenHands 接入远超预期 | 高 | 中 | §6.5 方案 A→B→C 时间盒；C 方案替换不破坏实验设计 |
| 2 | 任务集质量不过关（verifier 误判） | 中 | 高 | 校验清单 + 空轨迹/GT 双向测试 + W2 人工抽查 10 个 |
| 3 | 实验成本超预算 | 低 | 低 | 预算上限 + 低价 backbone + 失败不重跑（记录原因即可） |
| 4 | 框架间结果差异不显著 | 中 | 中 | 这本身就是结论；转到分难度/失败模式维度找差异，案例研究兜底 |
| 5 | judge 双模型分数分歧大 | 中 | 低 | 一致性报告 + 锚定人工分 + 分歧大时主表以 verifier 硬指标为准 |
| 6 | 时间超支 | 中 | 高 | 裁剪优先级：先保"最小完整闭环"（2 框架 × 1 模型 × 40 任务），dashboard 与案例研究可延后 |
| 7 | API 限流/不稳定 | 中 | 低 | LiteLLM 重试 + 指数退避 + 断点续跑（按 task_id 跳过已完成） |
| 8 | LiteLLM 未收录 deepseek-v4-flash | 中 | 低 | 走 openai 兼容端点（base_url + custom_llm_provider）或自定义 pricing 映射，不影响实验正确性 |

---

## 16. 交付物清单

1. **GitHub 公开仓库**：代码 + README（含架构图、复现指南）+ MIT + 样例数据
2. **任务集 v1.0**：40 任务（3 域 × 3 难度）+ 18 工具 mock 服务 + verifier 全测试
3. **实验报告**：6 组对照主表、分难度表、失败模式分布、案例研究、judge 质量报告
4. **Streamlit dashboard**：总览、轨迹回放、失败分析三页
5. **简历材料**：量化结果 + 项目 bullet（见 §17）

---

## 17. 简历产出（目标 bullet）

> **AgentEval：多框架 Agent 工具调用评测体系**（Python / FastAPI / LiteLLM / Streamlit）
> - 设计 40 个带可执行验证器的多轮工具调用任务（3 业务域 × 3 难度）与有状态 mock 工具服务
> - 实现统一评测 harness，接入自研 ReAct、smolagents、OpenHands 三类 Agent，支持预算控制与轨迹全量落盘
> - 完成 6 组（3 框架 × 2 backbone）对照实验，量化任务成功率 / 工具调用 F1 / 成本 / 延迟，并基于轨迹聚类定位 N 类失败模式
> - LLM-as-judge 双模型评分，一致性 xx%，与人工锚定相关性 xx

---

## 18. 参考资料（设计灵感来源，均在 README 中致谢）

| 项目 | 借鉴点 |
|---|---|
| τ-bench (sierra-research) | verifier 判定模式、有状态任务设计 |
| Berkeley BFCL | 工具调用任务的参数校验思路 |
| tool-eval-bench | 确定性场景构造方法 |
| langchain-ai/agentevals | 轨迹级评测器组织方式 |
| hodoscope | 多格式适配器与轨迹可视化思路 |
| SWE-bench | 报告结构与 leaderboard 呈现 |

---

## 19. 后续 Roadmap（本期之后）

- **v2（RL 闭环）**：以评测反馈（verifier + judge 分）为 reward，对 Qwen2.5-1.5B/3B 做 GRPO 微调，展示成功率提升曲线——串联三星（训练/数据）与云知声（RL/Agent）经历
- v3：任务域扩展（代码、GUI）、更多框架（LangGraph Agent 等）、CI 集成、在线 leaderboard
