"""judge.rubric —— LLM-as-Judge 评分标准（§8.2，1~5 分制）。

RUBRIC 是唯一权威定义：prompts 构造 system 提示时从这里取全文，
保证"评分标准与代码所见一致"。分数含义：
- 5：目标完全达成，动作无冗余，无错误尝试；
- 4：目标达成，有轻微冗余（1 次无效调用）；
- 3：目标达成，但路径明显低效（≥2 次无效调用或走弯路）；
- 2：目标未达成，但方向基本正确（关键调用发生）；
- 1：目标未达成且路径错误（关键调用未发生/违反约束）。

注：本模块只有常量、无函数，供 judge/prompts.py 与展示层引用。
"""
from __future__ import annotations

# 分数 → 中文定义（§8.2 原表）。字典序即"得分档位"，勿改动文案。
RUBRIC: dict[int, str] = {
    5: "目标完全达成，动作无冗余，无错误尝试",
    4: "目标达成，有轻微冗余（1 次无效调用）",
    3: "目标达成，但路径明显低效（≥2 次无效调用或走弯路）",
    2: "目标未达成，但方向基本正确（关键调用发生）",
    1: "目标未达成且路径错误（关键调用未发生/违反约束）",
}

# 合法分数范围（评分输出协议校验用）
MIN_SCORE = 1
MAX_SCORE = 5

__all__ = ["MAX_SCORE", "MIN_SCORE", "RUBRIC"]
