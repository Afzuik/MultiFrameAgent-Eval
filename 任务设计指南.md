# AgentEval 任务样本设计指南

> 配套《项目方案.md》§4 使用。核心原则一句话：
> **先想"这个任务要考什么失败模式"，再写任务。每个任务 = 一个设计意图（陷阱）× 一个可检测的失败信号。**

---

## 1. 难度从哪来：七个难度维度

任务难度不是"题目写得长"，而是叠加了哪些维度：

| # | 维度 | 说明 | 示例 |
|---|---|---|---|
| 1 | 轮次/步骤数 | 需要的工具调用次数 | 1 次查询 vs 查→订→改 3 步 |
| 2 | 信息依赖 | 后续步骤需要前序结果 | 先 `search_flights` 拿到 flight_id 才能 `book_flight` |
| 3 | 条件分支 | 结果取决于中间判断 | 超预算 → 换方案；符合 → 直接订 |
| 4 | 约束推理 | 权限/业务规则 | 退款前必须 `verify_identity`；改签要补差价 |
| 5 | 干扰信息 | goal 或工具返回中的冗余 | goal 里问"顺便看下天气"但工具集里没有天气工具 |
| 6 | 隐式需求 | 没明说但必须做 | "退款"隐含"先找到订单号" |
| 7 | 错误恢复 | 首次调用失败后的修正 | 参数格式错被拒后改用正确格式 |

**难度档 = 维度组合**：

| 档 | 典型组合 | 数量 |
|---|---|---|
| L1 | 维度 1（±5 干扰） | 15 |
| L2 | 维度 1+2+3 | 15 |
| L3 | 维度 1+2+3+4（±5/6/7 任选叠加） | 10 |

---

## 2. 陷阱库（按 §10 失败模式组织）

每个机关都对应一个**可检测的失败信号**——这是任务设计与指标层联动的关键。

### 2.1 工具选择错误类

| 机关 | 写法 | 检测 |
|---|---|---|
| 相似工具混淆 | 提供 `search_flights` 和 `search_hotels`，goal 只要机票 | F1 Precision |
| 越权工具 | goal 需要"取消"，但只有 `refund_reservation`（含退款）可用，正确行为是用 refund 而非伪造 cancel | forbidden_calls |

### 2.2 参数错误类

| 机关 | 写法 | 检测 |
|---|---|---|
| 格式陷阱 | 日期必须是 ISO（`2026-10-12`），goal 里用中文日期（"10 月 12 日"） | required_calls 的 args_contain |
| 单位陷阱 | 价格单位：goal 说"预算 12"，工具返回 1200（分） | answer_checks |
| 大小写/编码 | 机场码必须大写（`PEK` 而非 `pek`） | required_calls |

### 2.3 规划失败类

| 机关 | 写法 | 检测 |
|---|---|---|
| 缺步骤 | "退款"但 goal 不给订单号 → 必须先 list 再 refund | required_calls 两步 |
| 顺序依赖 | 先 `book_flight` 后 `search_flights` 是错的（book 需要 flight_id） | 工具返回错误 → 轨迹中出现失败调用 |

### 2.4 权限/规则违反类

| 机关 | 写法 | 检测 |
|---|---|---|
| 身份验证 | `refund_reservation` 声明 requires_auth，未验证返回 403 | forbidden_calls + 轨迹中的 403 响应 |
| 业务规则 | 改签只允许起飞前 24 小时，任务时间设在不满足窗口内 | final_state 检查改签失败 |

### 2.5 干扰信息类

| 机关 | 写法 | 检测 |
|---|---|---|
| goal 冗余 | "顺便告诉我明天天气"（无天气工具）→ 正确行为是完成主任务并说明无此能力 | 不触发任何检查，但 judge 轨迹分 |
| 返回冗余 | 查询返回 10 条航班，只有 2 条上午 → 答案必须只含上午航班 | answer_checks 的 not_contains |

### 2.6 错误恢复类（L3 专属）

| 机关 | 写法 | 检测 |
|---|---|---|
| 首次必败 | 工具首次调用返回参数错误提示，第二次正确参数才成功 | 轨迹中"失败调用→成功调用"序列 |

---

## 3. 手把手：一个 L2 任务的完整设计过程

### 3.1 用户故事

> 小王要在 10 月 12 日上午从北京飞上海，预算 1200 元。

### 3.2 选择机关与写 goal

选中的机关：**条件分支**（维度 3）+ **信息依赖**（维度 2）+ **参数格式**（维度 7 的轻量版）。

航班数据设计（关键是数量关系）：

| flight_id | 起飞 | 舱位 | 价格 |
|---|---|---|---|
| MU5101 | 08:30 | 经济舱 | ¥980 ← 唯一"上午 + 符合预算" |
| MU5103 | 09:45 | 商务舱 | ¥1450（上午但超预算） |
| MU5105 | 13:20 | 经济舱 | ¥760（最便宜但是下午） |

goal 定稿：

> 帮我订 10 月 12 日上午北京到上海的航班，预算 1200 元。如果上午的航班都超过预算，就改订当天最便宜的航班，并告诉我最终价格。

陷阱生效方式：贪心的 Agent 会直接选最便宜的 MU5105（¥760）→ 违反"上午"约束；不会比较的 Agent 会订 MU5103（超预算）→ 违反预算。两条错误路径都能被 verifier 抓住。

### 3.3 初始状态

```json
{ "user_id": "u_42", "vip": false }
```

### 3.4 Verifier 四原语

| 类型 | 内容 | 抓什么错 |
|---|---|---|
| required_calls | `search_flights`（date=2026-10-12, from=PEK, to=SHA） | 没查就订 |
| required_calls | `book_flight`（flight_id=MU5101） | 订了错航班 |
| final_state_checks | `reservations.u_42.0.flight_id == "MU5101"`、`status == "booked"` | 贪心选下午、超预算 |
| answer_checks | 包含 "980" | 没报价格 |

### 3.5 完整任务 JSON

```json
{
  "task_id": "travel_014",
  "domain": "travel",
  "user_goal": "帮我订 10 月 12 日上午北京到上海的航班，预算 1200 元。如果上午的航班都超过预算，就改订当天最便宜的航班，并告诉我最终价格。",
  "difficulty": "L2",
  "design_note": "考察条件分支：贪心选最便宜会命中下午航班（违反上午约束），不比较会订超预算航班",
  "min_expected_turns": 2,
  "max_steps": 12,
  "initial_state": { "user_id": "u_42", "vip": false },
  "required_calls": [
    { "tool": "search_flights", "args_contain": { "date": "2026-10-12", "from": "PEK", "to": "SHA" } },
    { "tool": "book_flight", "args_contain": { "flight_id": "MU5101" } }
  ],
  "forbidden_calls": [
    { "tool": "book_flight", "args_contain": { "flight_id": "MU5105" } }
  ],
  "final_state_checks": [
    { "path": "reservations.u_42.0.flight_id", "equals": "MU5101" },
    { "path": "reservations.u_42.0.status", "equals": "booked" }
  ],
  "answer_checks": [
    { "contains": "980" },
    { "not_contains": "MU5105" }
  ],
  "ground_truth_plan": [
    { "tool": "search_flights", "args": { "date": "2026-10-12", "from": "PEK", "to": "SHA" } },
    { "tool": "book_flight", "args": { "flight_id": "MU5101", "class": "economy" } }
  ],
  "judge_rubric_hint": "重点考察：是否比较价格并做出正确条件分支"
}
```

### 3.6 对应 Python verifier

```python
def verify_travel_014(calls: list[ToolCall], final_state: dict, answer: str):
    findings = []
    if not has_call(calls, "search_flights",
                    {"date": "2026-10-12", "from": "PEK", "to": "SHA"}):
        findings.append("missing_search")
    if not has_call(calls, "book_flight", {"flight_id": "MU5101"}):
        findings.append("wrong_or_missing_booking")
    res = final_state["reservations"]["u_42"]
    if not res or res[0]["flight_id"] != "MU5101":
        findings.append("greedy_wrong_flight")
    if "980" not in answer:
        findings.append("answer_missing_price")
    return (not findings, findings)   # (passed, failure_signals)
```

### 3.7 自检记录（写入任务文件注释）

- [x] 空轨迹 → fail（所有 required 未命中）
- [x] GT 路径 → pass
- [x] 错误路径 1（订 MU5105）→ 抓 greedy_wrong_flight
- [x] 错误路径 2（订 MU5103）→ 抓 wrong_or_missing_booking
- [x] goal 未泄漏任何航班号

---

## 4. 反面教材：三个坏任务及修法

### 4.1 歧义任务 ❌

> 帮我订一张便宜的机票。

"便宜"无定义：和谁比？低于多少？→ verifier 写不出来。
**修法**：给量化约束——"预算 1200 元，若超预算则订最便宜的"。

### 4.2 答案泄漏任务 ❌

> 帮我退掉订单 ORDER_20261012_001 的机票。

关键参数（订单号）直接写在 goal 里 → 信息检索环节消失，任务退化成参数搬运。
**修法**：隐式指代——"帮我退掉我最近订的那张机票"（需先 `list_reservations` 检索）。

### 4.3 不可验证任务 ❌

> 请友好地帮用户解决问题。

"友好"是主观标准，verifier 无法判定。
**修法**：换成可检查的约束（"如果无法完成，必须明确告知用户原因"→ 用 judge 补充评分，但硬指标不依赖主观项）。

---

## 5. 三域种子菜单（15 个种子选题，直接开工）

### travel（差旅）

| 种子 | 档 | design_note |
|---|---|---|
| 查上午航班 | L1 | 干扰：答案过滤下午航班 |
| 订符合预算的上午航班 | L2 | 条件分支（§3 示例） |
| 超预算改订最便宜 | L2 | 分支 + 全量比较 |
| 忘订单号退票退款 | L3 | 两步检索 + 身份验证 + 状态依赖 |
| 两人多段行程拼接 | L3 | 多工具序列 + 多状态依赖 |

### shop（电商）

| 种子 | 档 | design_note |
|---|---|---|
| 查订单物流状态 | L1 | 单工具 |
| 修改收货地址 | L1 | 参数：地址格式校验 |
| 申请退款（需身份验证） | L2 | 权限 + 状态流转 |
| 补开发票 | L2 | 信息依赖：先查订单详情取金额 |
| 月度消费汇总报告 | L3 | 多步查询 + 汇总 + 报告格式要求 |

### analytics（数据查询）

| 种子 | 档 | design_note |
|---|---|---|
| 单表计数 | L1 | 单工具 |
| 条件过滤统计 | L1 | 参数：过滤字段选择 |
| 跨表 join | L2 | 信息依赖：先查 schema 再查数 |
| 环比趋势对比 | L2 | 两次查询 + 对比逻辑 |
| 生成周报 | L3 | 多跳取数 + 报告结构要求 + 干扰指标 |

---

## 6. LLM 扩写模板与流程

### 6.1 扩写 prompt 模板

```
你是评测任务生成器。基于下面的种子任务，生成 3 个变体。

【变化维度】（每次只改变以下字段，任务结构与考察点不变）：
- user_id / VIP 状态
- 日期、出发时段、目的地机场
- 预算与价格数值（保持"符合/超出预算"的数量关系不变）

【禁止事项】：
- 不得改变 design_note 中声明的失败模式
- 不得新增或删除工具
- 不得在 user_goal 中泄露 ground_truth_plan 的关键参数
- 不得产生"歧义/泄漏/不可验证"三类坏任务（见指南 §4）

【种子任务】：
{种子 JSON}

【输出格式】：JSON 数组，每项为完整任务对象（同种子 schema），
并同步更新 design_note 与 verifier 四原语。
```

### 6.2 生成后人工流程

1. 逐条过 §4.5 校验清单
2. 跑 `tasks/validate.py`（空轨迹 fail + GT pass 双向测试）
3. 人工检查一条"错误路径"确实被抓到（防止 verifier 假阴性）
4. 填写 design_note → 入库 → 写 verifier 单测

---

## 7. 入库自检清单（每任务必过）

- [ ] design_note 写明了考察的失败模式，且对应 §10 分类表中的一类
- [ ] goal 无歧义、无泄漏、硬指标可判定
- [ ] 四原语齐全（required / forbidden / final_state / answer 至少用上三类）
- [ ] 空轨迹 fail、GT 路径 pass、至少一条错误路径被正确捕获
- [ ] verifier 单元测试已写（正向 + 反向）
- [ ] 与工具注册表一致（工具名、参数 schema 无出入）
- [ ] answer_checks 对自然表述变体鲁棒（v1.1 新增）：纯数字检查必须容忍千分位/小数格式（verifier 已支持数值匹配，勿依赖固定格式子串）；any_of 关键词需覆盖常见措辞变体（如"已为您取消"应能被"取消"命中）；主要判定权重应放在调用/终态类检查而非措辞
- [ ] 工具描述覆盖可推断的返回字段（v1.1 新增）：Agent 只能从 description 推断工具能力，缺失关键返回字段说明会导致"有工具却不用"（如 get_order_status 必须声明返回物流单号/收货地址/发票）
