# 实验矩阵（§9.1）。W2 启用 R1（react）与 S1（smolagents）两组 × 全量三域 40 任务。
# 后续里程碑追加 R2/S2/O1/O2 组。
runs:
  - group: R1
    framework: react
    model: deepseek-v4-flash
    domains: [travel, shop, analytics]
    note: "ReAct × deepseek-v4-flash，全量 40 任务"
  - group: S1
    framework: smolagents
    model: deepseek-v4-flash
    domains: [travel, shop, analytics]
    note: "smolagents × deepseek-v4-flash，全量 40 任务"
